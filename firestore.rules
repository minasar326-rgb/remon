rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد وصول متزامنة لنظام حضور كنيسة أبي سيفين
    // تتيح المزامنة اللحظية بين آلاف الأجهزة بدون قيود
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
